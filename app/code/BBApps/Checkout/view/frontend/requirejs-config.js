/**
 * @type {{map: {"*": {discountCode: string, shoppingCart: string, regionUpdater: string, sidebar: string, checkoutLoader: string, checkoutData: string, proceedToCheckout: string}}}}
 */
var config = {
    map: {
        '*': {
            custom_sidebar: 'BBApps_Checkout/js/custom_sidebar'
        }
    }
};
